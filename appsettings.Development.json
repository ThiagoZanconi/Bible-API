{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Server=${MYSQLHOST};Port=${MYSQLPORT};Database=backendproject_schema;User=${MYSQLUSER};Password=${MYSQLPASSWORD};"
  }
}
